# Local Plugins

프로젝트 로컬 opencode plugin을 두는 위치입니다.

현재 plugin:

- `graphify.js`: `graphify-out/graph.json`이 있을 때 graphify query 사용을 상기시키는 OpenCode hook

새 plugin은 출처, 라이선스, hook 종류, 네트워크 사용, 생성 파일, 권한 범위를 확인한 뒤 추가합니다.
